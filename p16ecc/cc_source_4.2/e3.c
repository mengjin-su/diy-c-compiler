int x, y, z[4];
long;

static foo(int a, char b, long *c)
{
    if ( a == b ) 
        *c = 0;
    else
        *c = 1;

    if ( a > 0 )
    if ( a >= 1 ) return 1;
    else return 2;
    return 3;
}
